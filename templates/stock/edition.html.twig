{% extends 'base.html.twig' %}

{% block title %}Edit Stock{% endblock %}

{% block body %}
    <div class="container container2">
        <h1>Modifier le stock</h1>

        <div style="display: block">{{ include('stock/form.html.twig', {'button_label': 'Modifier'}) }}</div>
        <div style="display: inline-block"> <br><a class="btn btn-primary" href="{{ path('stockage') }}">Retour à la liste</a></div>
        <div style="display: inline-block">{{ include('stock/supprimer.html.twig') }}</div>

    </div>
{% endblock %}
